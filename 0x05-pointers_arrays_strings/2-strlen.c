#include<stdio.h>
#include"holberton.h"
/**
* _strlen - 
* @s: int s
* Return: Always 0 (Success)
*/
int _strlen(char *s)
{
	char a, b;
	int i;

	b = '\\' +  '0';
	for (i = 0; a == b  ; i++)
	{
	a = s[i];
	printf("%c", a);
	}
	return(i);
}
